#include "Pods-Lookback.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_LOOKBACK_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Lookback" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Lookback" "${PODS_ROOT}/Headers/TestFlightSDK"
OTHER_LDFLAGS = -ObjC ${PODS_LOOKBACK_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}